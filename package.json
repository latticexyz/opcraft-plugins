{
  "name": "opcraft-plugins",
  "version": "1.0.0",
  "main": "index.ts",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "prepare": "husky install",
    "start": "esbuild ./plugins/* --bundle --servedir=www",
    "build": "esbuild ./plugins/* --bundle --outdir=dist"
  },
  "dependencies": {},
  "devDependencies": {
    "esbuild": "^0.15.12",
    "eslint": "^8.26.0",
    "husky": "^8.0.1",
    "lint-staged": "^13.0.3",
    "prettier": "^2.7.1",
    "typescript": "^4.8.4"
  },
  "lint-staged": {
    "*.ts": "eslint --cache --fix",
    "*.{ts,css,md,sol}": "prettier --write"
  }
}
